{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Helper preset for Python apps projects",
  "packageRules": [
    {
      "matchDepTypes": ["dependencies"],
      "excludeDepNames": ["python"],
      "rangeStrategy": "pin"
    }
  ]
}
